# Phase 0 + 1 foundation

- Frontend and backend live in the same repository but are packaged and deployed independently.
- Public content is sourced from repository MDX files under `content/`.
- Structured persistence uses SQLite and vector persistence uses FAISS snapshots on the API side.
- `/lab` and `/chat` are public teaser routes in this phase; the real chatbot remains out of scope.

